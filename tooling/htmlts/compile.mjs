#!/usr/bin/env node
/**
 * htmlts.compile (STUB)
 * Deterministic placeholder until the HTML+TS compiler is integrated.
 *
 * Behavior:
 * - Always exits 0
 * - Emits a stable message (no timestamps, no randomness)
 * - Performs no file writes
 */

console.log("[htmlts.compile] STUB: tool not implemented yet (exits 0).");
process.exit(0);
