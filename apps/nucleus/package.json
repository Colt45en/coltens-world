{
  "name": "apps-nucleus",
  "private": true,
  "type": "module",
  "dependencies": {
    "@world-engine/artifacts": "workspace:*",
    "@world-engine/engine": "workspace:*",
    "@world-engine/flowstate": "workspace:*",
    "@world-engine/ledger": "workspace:*",
    "@world-engine/ledger-contracts": "workspace:*",
    "@world-engine/lego-prefab": "workspace:*",
    "@world-engine/physics-contract": "workspace:*",
    "@world-engine/protocol": "workspace:*",
    "better-sqlite3": "^9.2.2",
    "chokidar": "^3.6.0",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "@vitest/ui": "^2.1.8",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "test:watch": "vitest watch"
  }
}
