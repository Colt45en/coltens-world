/**
 * ⚠️ SIMULATION MODULE - DISABLED
 *
 * This module was disabled because it depends on ArchetypeWorld and SpatialGrid
 * from @world-engine/engine which are not currently exported.
 *
 * It was also not being used in nucleus/src/index.ts.
 *
 * If you need simulation features, restore from git history and ensure
 * ArchetypeWorld/SpatialGrid are properly exported from packages/engine.
 */

export { };
