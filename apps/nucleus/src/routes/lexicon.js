/**
 * ⚠️ LEXICON ROUTES - DISABLED TO UNBLOCK BUILD
 *
 * This route handler was disabled because it requires:
 * - fastify (not a dependency in nucleus)
 * - better-sqlite3 (not a dependency)
 * - Was not being imported or used in index.ts
 *
 * Original implementation is available in git history.
 * To restore and use in the future:
 *
 * 1. Install dependencies:
 *    pnpm add -D better-sqlite3 @types/better-sqlite3 fastify @types/fastify
 *
 * 2. Restore original file from git history:
 *    git checkout HEAD -- apps/nucleus/src/routes/lexicon.ts
 *
 * 3. Integrate into index.ts if needed
 */
export {};
